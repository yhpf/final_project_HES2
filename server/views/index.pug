//-
	pug template for first page for input
	layout in layout.pug
	forminput as output in todoitems.pug
	I can also use action in the form (by name and method) to get form input in URL to another page

extends layout

block content
	h1 Welcome to your To-do list!
	if flashMsg.length > 0
			//-bootstrap warning message
			.alert.alert-danger <strong></strong>#{flashMsg} 
	form.inputform(name="priorities", method="POST" action="/index" enctype="multipart/form-data")
		p Please add all the stuff that you need to do...
			div.form-group
				label.indexformtext(for='name') Title:
				br
				input#name.inputfield(type='text', placeholder='my todo item' name='title', required='')
			div.form-group
				label.indexformtext(for='comment') Description:
				br
				textarea#comment.inputfield(type='text', placeholder='This I have to do...' name='description', required='' height='100')
			div.form-group
				label.indexformtext(for='file') Attachment:
				p (Supported file types .jpg .jpeg .txt .doc .pdf .xls)
				input#file.inputfield(type='file', name='file', required='')
			div.form-group
				label.indexformtext(for='date') Deadline:
				br
				input#date.inputfield(type='date', name='duedate', required='')
			input.submit(type='submit', value='Submit')
			input.reset(type='reset', value='Reset')

	h3 Priorities for #{date}

	include todoitems

	a(href="/todolist")
		button.nextpage(type="button") View Done to-do items